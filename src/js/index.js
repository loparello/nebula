/* Use this file to load global dependencies like jQuery or Sass styles.
*  This file will get compiled into 'main.js' and loaded in the template head. 
*/

// Loads styles entry point.
import '../sass/main.scss';

/* Loads jQuery and the Bootstrap jQuery plugin which provides support
 * for JavaScript based Bootstrap features such as modals and tabs.
 * If you want jQuery or Bootstrap uncomment the script below. 
 */
// try {
//     window.Popper = require('popper.js').default;
//     window.$ = window.jQuery = require('jquery');

//     require('bootstrap');
// } catch (error) {
//     console.error(error);
// }

